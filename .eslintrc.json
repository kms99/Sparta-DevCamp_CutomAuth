{
  "root": true,
  "overrides": [
    {
      "files": ["packages/backend/**/*.*", "packages/frontend/**/*.*"],
      "extends": ["eslint:recommended"],
      "env": {
        "node": true
      },
      "rules": {
        // 여기에 공통 규칙을 설정합니다.
      }
    },
    {
      "files": ["packages/frontend/next.config.js"],
      "extends": ["eslint:recommended", "plugin:@next/next/recommended"],
      "env": {
        "browser": true,
        "node": true
      },
      "rules": {
        // 여기에 공통 규칙을 설정합니다.
      }
    }
  ]
}
